# Brotherhood Seal: 015cb320
name: 🛡️ Advanced Secret Scanner

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  gitleaks:
    name: 🔍 Gitleaks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  static-site-scan:
    name: 🌐 Static Site Scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: 🔍 Public Site Secrets
        run: |
          PATTERNS="api[_-]?key.*=|ghp_|firebase.*="
          if grep -rEi "$PATTERNS" . --include="*.html" --include="*.js" \
             --include="*.json" --exclude-dir=_site 2>/dev/null; then
            echo "❌ API key in public site"; exit 1
          fi
          echo "✅ Public Site: SECURE"
